<div class="content content-about">
  <%- partial('_partial/component/page-header', {location: __('location.about')}) %>

  <ul class="about-list">
    <% theme.about.forEach(function(item, i){ %>
      <li class="about-item">
        <% if(item.icon) {%>
          <i class="icon <%= item.icon%> dot dot-<%= i % 5%>"></i>
        <% } else { %>
          <i class="dot dot-<%= i % 5%> "></i>
        <% }%>

        <% if(typeof item.text_value === 'object') {%>
          <% item.text_value.forEach(function(item) {%>
            <div class="text"><%= item%></div>
          <% });%>
        <% } else { %>
          <div class="text">
            <span><%= item.text_key || ''%></span>
            <% if(item.text_key) {%>
              <a rel="noopener noreferrer" target="_blank" class="text-value-url" href="<%= item.text_value_url || ''%>"><%= item.text_value || ''%></a>
            <% } else { %>
              <span><%= item.text_value || ''%></span>
            <% }%>
          </div>
        <% }%>

      </li>
    <% }) %>
  </ul>
  
<div style="text-align:center;color:#ccc;font-weight:300;margin:10px auto 0;padding:10px 0 0;overflow:hidden">
	© 2018 - 2018 <a style="color:#ccc" href="/_" >sustcoder</a> | produce by <a style="color:#ccc"  href="sustcoder.github.io" target="_blank">liyz</a>	| 本站总访问量 <span id="busuanzi_value_site_pv"></span> 次
</div>

</div>

<%- partial('_partial/component/back-top', {showBacktop: theme.backtop.about}) %>
